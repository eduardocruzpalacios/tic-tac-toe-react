import Styled from 'styled-components';
import { space120 } from '../../../common/styleUtils';

export const MainStyled = Styled.main`
  align-items: center;
  display: flex;
  justify-content: center;
  min-height: calc(100vh - ${space120});
`;
